# Implementation Plans

Generated by the improve skill on 2026-07-05, against commit `7bff3dd`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Repo ground rules that apply to **every** plan: no automated tests, ever
(deliberate maintainer policy — QA is manual); this is Next.js 16 (consult
`node_modules/next/dist/docs/` before assuming conventions); comments and
commit messages in Spanish; `npm run build` is the type gate until plan 007
adds `npm run typecheck` (a stale `.next/dev/types` file makes bare
`tsc --noEmit` false-fail).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Cerrar el bypass `?secret=` del cron en producción | P1 | S | — | DONE (branch `advisor/001-cron-secret-query-bypass`, unmerged) |
| 002 | Rol admin server-side en acciones de administración | P1 | M | — | TODO |
| 003 | Versionar la idempotencia del email de reprogramación | P1 | S | — | TODO |
| 004 | Local→UTC DST-correcto en acciones admin de calendario | P1 | S | — | TODO |
| 005 | `normalize()` sin mutar los intervalos del caller | P1 | S | — | TODO |
| 006 | Consumo atómico del token de reseteo de contraseña | P1 | S | — | TODO |
| 007 | Higiene de toolchain y entorno (deps/typecheck/env/CI/deploy) | P2 | M | — | TODO |
| 008 | Cabeceras de endurecimiento HTTP | P2 | S | — | TODO |
| 009 | `getCurrentSalon` a `lib/salons/` con `cache()` | P2 | S | 002 (orden) | TODO |
| 010 | Índice `(salon_id, starts_at)` en `booking_items` + pool acotado | P2 | S | — | TODO |
| 011 | Realinear README/MEMORY/AGENTS con el SaaS Postgres | P2 | M | — | DONE (rama `claude/app-roadmap-docs-yu0574`; PLAN*.md archivados en `docs/archive/`) |
| 012 | (spike) Diseño `salons.status` + signup self-serve | P3 | M | — | TODO |
| 013 | (spike) Diseño enlace mágico cancelar/reprogramar | P3 | M | 006 (leer), 012 (leer si existe) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **009 after 002**: plan 002 edits the same action files that 009 re-imports
  from; landing 002 first avoids rebase churn. Soft ordering, not a hard
  dependency.
- **013 reads 006 and 012**: the single-use-token consumption pattern from
  006 is a design constraint for the manage-link tokens; 012's `salons.status`
  gating decides whether suspended salons keep their manage links alive.
- **012 absorbs the `/setup` abuse finding**: the open unauthenticated
  `/setup` (unthrottled argon2 + unlimited tenant creation) is fixed as part
  of the signup design, not as a standalone patch.
- **AGE-001 cubre la base de ciclo de vida de 012**: `salon_lifecycle`, el trial
  inicial, el backfill y las transiciones ya pertenecen a la historia de Product
  OS. El alcance pendiente del spike 012 es el diseño de signup y el re-cableado
  de `/setup`; no debe volver a diseñar un `salons.status` alternativo.
- **007's operator follow-up matters**: `AUTH_SECRET`/`AUTH_TRUST_HOST` are
  absent from `config/deploy.yml` today; the code change adds the wiring but
  the operator must put the secret in `.kamal/secrets` and `kamal env push`.
- 001–006 are independent of each other and can be executed in parallel
  worktrees.

## Findings considered and rejected / deferred (do not re-audit)

- **Split large files** (`lib/availability/booking.ts` 556 ln,
  `lib/employees/actions.ts` 549 ln, `lib/db/schema.ts` 596 ln): rejected —
  size alone isn't a defect; no churn-pain evidence yet.
- **`app_users` SELECT policy `using (true)`**: deferred — documented
  tradeoff in `drizzle/0003_rls.sql:16-19` (login needs pre-tenant reads).
  Optional hardening (SECURITY DEFINER lookup + tenant-scoped SELECT) if the
  threat model tightens.
- **Reminder batch opens a transaction per booking**
  (`lib/email/triggers/send-reminders.ts:59,84`): deferred — bounded (~2h
  window per run); revisit if cron duration grows. Fix must keep Resend
  calls outside any DB transaction.
- **Slot-grid drift in `chunkBySlot`** (`lib/availability/intervals.ts:81-88`):
  deferred investigation — slots can come off the granularity grid after
  subtracting off-grid holes (e.g. 20-min service on a 15-min grid).
  Cosmetic (slots remain valid). Investigate whether real durations trigger
  it before designing a snap-to-grid.
- **Action result-shape divergence + raw `(e as Error).message` to client**
  (~15 sites, e.g. `lib/salons/actions.ts:67`): deferred — worthwhile M-size
  cleanup (`ActionResult` type + `toActionError` helper) but below the
  cut line this run.
- **Duplicated slug modules** (`lib/employees/slug.ts` ≈
  `lib/services/slug.ts`): deferred — extract shared `slugify` +
  `resolveUniqueSlug` when a third sluggable entity appears.
- **SQLite-era comments justifying `.slice(0,5)` trims**
  (`lib/employees/queries.ts:295`, `lib/salons/queries.ts:115`): deferred —
  clean up opportunistically; verify column types before removing trims.
- **Landing `"use client"` components**: not a finding — they genuinely need
  the client (IntersectionObserver/scroll/count-up), no heavy deps.
- **Pagination on admin lists**: not a finding — no unbounded list pages
  exist; booking reads are range-bounded.
- **"Dead deps"**: not a finding — `sharp`, `@dnd-kit/*`, `react-day-picker`
  all imported; `lucide-react@^1.14` is a real version; `next-auth@5-beta`
  has no stable v5 to move to.
- **Missing tests**: excluded by explicit maintainer policy (QA manual).

## Direction backlog (not planned this run)

Per the pivot doc's order `A → (B ∥ C) → D`, after spike 012: Stripe billing
+ gating (sub-project C, L) and minimal superadmin (D, M) remain roadmap-
named and unbuilt. Spike them like 012 when their turn comes.
